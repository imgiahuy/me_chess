# Slick database configuration

# H2 in-memory database configuration (for testing)
h2mem {
  profile = "slick.jdbc.H2Profile$"
  db {
    connectionPool = disabled
    driver = "org.h2.Driver"
    url = "jdbc:h2:mem:chess;DB_CLOSE_DELAY=-1"
    user = "sa"
    password = ""
  }
}

# H2 file-based database configuration (for development/production)
h2file {
  profile = "slick.jdbc.H2Profile$"
  db {
    driver = "org.h2.Driver"
    url = "jdbc:h2:./data/chess;DB_CLOSE_DELAY=-1"
    user = "sa"
    password = ""
    connectionPool = "HikariCP"
    numThreads = 10
    maxConnections = 10
    minConnections = 2
    queueSize = 1000
    connectionTimeout = 30000
    idleTimeout = 600000
    maxLifetime = 1800000
    leakDetectionThreshold = 60000
  }
}

# PostgreSQL database configuration (for production)
postgresql {
  profile = "slick.jdbc.PostgresProfile$"
  db {
    driver = "org.postgresql.Driver"
    url = "jdbc:postgresql://localhost:5432/chess"
    user = "chess_user"
    password = "chess_password"
    connectionPool = "HikariCP"
    numThreads = 10
    maxConnections = 20
    minConnections = 5
    queueSize = 1000
    connectionTimeout = 30000
    idleTimeout = 600000
    maxLifetime = 1800000
    leakDetectionThreshold = 60000
  }
}

# MongoDB database configuration (for production)
mongodb {
  host = "localhost"
  port = 27017
  database = "chess"
}

# Redis configuration (for caching)
redis {
  host = "localhost"
  port = 6379
  database = 0
  password = null
  timeout = 2000
  pool {
    maxTotal = 128
    maxIdle = 64
    minIdle = 16
  }
}

# Default database configuration
slick.db = ${postgresql}
